{% extends 'base_restau.html.twig' %}

{% block title %}notre Carte{% endblock %}

{% block body %}


<div class="myImages">
    <img src="{{ asset('ASSETS/data/homeRoom.jpg') }}" class="myImages" alt="mon image" srcset="">
</div>

<h1 class="text-center">Notre Carte</h1>

<div class="my-divider my-4"></div>

{# LOOP ON THE DIFFERENT MEALS -------------------------------------- #}
{# "id, category_id, meal_name, description, price, category_name, sub_category, range_num" #}

{# initialize rupture variable #}
{% set rupture = '*' %}

{# loop on Meals #}
{% for line in lines %}

  {# show category when and only when it has changed #}
  {% if rupture != line.category_name %}
    <h2>
        {{ line.category_name }}
        {{ line.sub_category }}
    </h2>
    {% set rupture = line.category_name %}
  {% endif %}

  {# show meal and detail #}
  <article class="text-center ">
      <div>
          {{ line.meal_name }}
      </div>
      <div>
          {{ line.description }}
      </div>
      <div>
          {{ line.price }} €
      </div>
  </article>

{% endfor %}
{# /LOOP ON THE DIFFERENT MEALS ------------------------------------- #}

<div class="my-divider my-4"></div>

<article class="container-fluid">

  <div class="row justify-content-center">
    <div class="col-12 col-sm-9 col-lg-6 px-5  ">
      
      <h2 class="mt-0 mb-2">
        Pour réserver...
      </h2>
      <p>      
      Menu ou carte, pour réserver cliquez sur le bouton :
      </p>
    </div>
  </div>

  <div class="row justify-content-center ">
    <div class="col text-center py-0">        
      <!-- <a class="nav-link ps-2 pe-0 p-sm-2 mx-lg-3 myBtonBook" href="booking.php"> -->
      <a class="bton nav-link d-block p-2 myBtonBook mx-auto" href="booking.php" role="button">
        RESERVER
      </a>
    </div>
  </div>      

  <div class="row justify-content-center">
    <div class="col-12 col-sm-9 col-lg-6 py-5 align-self-center myStickers">
      <img src="ASSETS/IMG/logo.png" class="myImages w-100" alt="" srcset="">
    </div>
  </div>

</article>



{% endblock %}
